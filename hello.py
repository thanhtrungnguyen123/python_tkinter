from tkinter import*

root = Tk()
label = Label(root, text='Hello world')
label.grid(row=0, column=1)
label = Label(root, text='I like chicken and cheese')
label.grid(row=0, column=1)

root.mainloop()